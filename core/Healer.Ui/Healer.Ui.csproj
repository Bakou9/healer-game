<Project Sdk="Microsoft.NET.Sdk">

  <!-- Logique d'interface pure (formatage tronqué, mise en page, ciblage) : aucune dépendance à Unity. -->
  <PropertyGroup>
    <TargetFramework>netstandard2.1</TargetFramework>
    <LangVersion>9.0</LangVersion>
    <Nullable>enable</Nullable>
    <RootNamespace>Healer.Ui</RootNamespace>
    <AssemblyName>Healer.Ui</AssemblyName>
  </PropertyGroup>

</Project>
